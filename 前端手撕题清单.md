# 前端手撕题清单

> 用途：面试前快速过一遍高频手写题。建议每题至少能说清楚思路、边界条件、时间复杂度，并能在 10-20 分钟内写出可运行代码。

## JavaScript 基础实现

1. 手写 `call`
   - 考点：`this` 绑定、临时属性、参数透传、返回值。

2. 手写 `apply`
   - 考点：数组参数处理、`this` 为 `null` / `undefined` 时的默认绑定。

3. 手写 `bind`
   - 考点：函数柯里化、构造函数调用、原型继承、返回函数的 `this` 优先级。

4. 手写 `new`
   - 考点：创建对象、原型绑定、构造函数返回对象时的特殊处理。

5. 手写 `instanceof`
   - 考点：原型链遍历、边界值处理。

6. 手写 `Object.create`
   - 考点：原型对象、空原型对象、属性描述符扩展。

7. 手写 `Object.assign`
   - 考点：可枚举属性、Symbol 属性、浅拷贝、异常处理。

8. 手写浅拷贝
   - 考点：对象与数组处理、引用类型共享。

9. 手写深拷贝
   - 考点：循环引用、`Date`、`RegExp`、`Map`、`Set`、Symbol、函数、原型。

10. 手写数组扁平化 `flat`
    - 考点：递归、迭代、指定扁平层级、稀疏数组。

11. 手写数组去重
    - 考点：`Set`、对象哈希、`NaN`、对象引用。

12. 手写数组分组 `groupBy`
    - 考点：回调取 key、对象聚合、`Map` 聚合。

13. 手写数组乱序 `shuffle`
    - 考点：Fisher-Yates 洗牌算法、公平随机。

14. 手写 `map`
    - 考点：回调参数、稀疏数组、返回新数组、不修改原数组。

15. 手写 `filter`
    - 考点：回调返回值、稀疏数组、索引与原数组参数。

16. 手写 `reduce`
    - 考点：初始值、空数组异常、累加器传递。

17. 手写函数柯里化 `curry`
    - 考点：参数缓存、递归返回函数、固定参数长度。

18. 手写偏函数 `partial`
    - 考点：预置参数、占位符参数。

19. 手写组合函数 `compose`
    - 考点：从右到左执行、返回函数、同步链式调用。

20. 手写管道函数 `pipe`
    - 考点：从左到右执行、函数式编程。

21. 手写发布订阅模式 `EventEmitter`
    - 考点：`on`、`off`、`once`、`emit`、事件参数。

22. 手写观察者模式
    - 考点：Subject、Observer、依赖收集、通知更新。

23. 手写单例模式
    - 考点：闭包缓存实例、类静态属性。

24. 手写 LRU 缓存
    - 考点：哈希表加双向链表、容量淘汰、访问更新。

25. 手写 `JSON.stringify`
    - 考点：基本类型、对象数组递归、`undefined`、函数、Symbol、循环引用。

26. 手写 `JSON.parse`
    - 考点：词法解析、递归下降、字符串转义、安全性。

27. 手写模板字符串解析
    - 示例：`render("hello {{name}}", { name: "Tom" })`
    - 考点：正则匹配、路径取值、缺省值。

28. 手写对象路径访问 `get`
    - 示例：`get(obj, "a.b[0].c")`
    - 考点：路径解析、数组下标、默认值。

29. 手写对象路径设置 `set`
    - 示例：`set(obj, "a.b[0].c", 1)`
    - 考点：中间对象创建、数组路径处理。

30. 手写数据类型判断 `typeOf`
    - 考点：`Object.prototype.toString`、`null`、数组、日期、正则。

## 异步与 Promise

1. 手写 `Promise`
   - 考点：三种状态、状态不可逆、回调队列、链式调用。

2. 手写 `Promise.then`
   - 考点：返回新 Promise、值穿透、thenable 解析、异常捕获。

3. 手写 `Promise.catch`
   - 考点：错误透传、等价于 `then(null, onRejected)`。

4. 手写 `Promise.finally`
   - 考点：不改变原结果、等待 finally 中的异步逻辑。

5. 手写 `Promise.resolve`
   - 考点：普通值、Promise、thenable 对象。

6. 手写 `Promise.reject`
   - 考点：直接返回 rejected 状态。

7. 手写 `Promise.all`
   - 考点：结果顺序、任一失败立即失败、空数组。

8. 手写 `Promise.race`
   - 考点：最先 settle 的结果、空数组。

9. 手写 `Promise.allSettled`
   - 考点：收集 fulfilled / rejected 状态、全部完成后返回。

10. 手写 `Promise.any`
    - 考点：任一成功即成功、全部失败返回 `AggregateError`。

11. 手写并发请求控制器
    - 示例：最多同时执行 3 个请求。
    - 考点：任务队列、并发上限、完成后补位。

12. 手写异步任务调度器 `Scheduler`
    - 考点：队列、最大并发、Promise 链。

13. 手写 `retry`
    - 考点：失败重试、最大次数、重试间隔。

14. 手写带指数退避的 `retry`
    - 考点：延迟策略、最大延迟、抖动随机。

15. 手写 `sleep`
    - 考点：Promise 封装定时器。

16. 手写可取消 Promise
    - 考点：取消标记、`AbortController`、资源清理。

17. 手写请求超时控制
    - 考点：`Promise.race`、定时器清理、错误语义。

18. 手写异步串行执行
    - 考点：`reduce` 链式 Promise、结果收集。

19. 手写异步并行执行
    - 考点：`Promise.all`、错误处理。

20. 手写异步池 `asyncPool`
    - 考点：并发限制、结果顺序、异常中断。

21. 手写红绿灯任务
    - 示例：红灯 3 秒、绿灯 2 秒、黄灯 1 秒循环。
    - 考点：递归异步流程、定时器。

22. 手写微任务调度器
    - 考点：`queueMicrotask`、`MutationObserver`、Promise fallback。

## 防抖、节流与性能工具

1. 手写防抖 `debounce`
   - 考点：延迟执行、清除定时器、参数与 `this`。

2. 手写立即执行版防抖
   - 考点：首次触发立即执行、后续触发重新计时。

3. 手写带取消功能的防抖
   - 考点：`cancel` 方法、定时器清理。

4. 手写节流 `throttle`
   - 考点：时间戳版本、定时器版本、首尾触发。

5. 手写 `requestAnimationFrame` 节流
   - 考点：浏览器渲染帧、滚动事件优化。

6. 手写图片懒加载
   - 考点：滚动监听、`IntersectionObserver`、占位图。

7. 手写虚拟列表
   - 考点：可视区域计算、滚动偏移、占位高度、动态高度扩展。

8. 手写大文件分片上传
   - 考点：Blob 切片、并发上传、断点续传、秒传。

9. 手写前端缓存封装
   - 考点：过期时间、localStorage、JSON 序列化、容量清理。

10. 手写性能埋点 SDK
    - 考点：PV、UV、错误上报、资源性能、批量发送、失败重试。

11. 手写错误监控
    - 考点：`window.onerror`、`unhandledrejection`、资源加载错误。

## DOM 与浏览器

1. 手写 DOM 选择器简版 `$`
   - 考点：`querySelector`、节点集合、链式调用。

2. 手写事件委托
   - 考点：事件冒泡、`target`、`currentTarget`、选择器匹配。

3. 手写拖拽
   - 考点：鼠标事件、坐标计算、边界限制、性能优化。

4. 手写元素可见性判断
   - 考点：`getBoundingClientRect`、视口尺寸、滚动容器。

5. 手写滚动到底部判断
   - 考点：`scrollTop`、`clientHeight`、`scrollHeight`。

6. 手写回到顶部
   - 考点：平滑滚动、定时器、`requestAnimationFrame`。

7. 手写复制到剪贴板
   - 考点：Clipboard API、权限、降级方案。

8. 手写 URL 参数解析
   - 考点：`URLSearchParams`、重复 key、编码解码。

9. 手写 Cookie 操作
   - 考点：设置、读取、删除、过期时间、path、domain。

10. 手写简版路由
    - 考点：hash 路由、history 路由、监听变更、视图渲染。

11. 手写跨标签页通信
    - 考点：`storage` 事件、`BroadcastChannel`、SharedWorker。

12. 手写 ajax
    - 考点：XMLHttpRequest、状态码、超时、错误处理。

13. 手写 fetch 封装
    - 考点：拦截器、超时、统一错误、请求取消。

14. 手写文件预览
    - 考点：FileReader、Object URL、内存释放。

## CSS 与布局手撕

1. 实现水平垂直居中
   - 考点：flex、grid、absolute + transform、table-cell。

2. 实现两栏布局
   - 考点：固定侧边栏、自适应主区域、flex、grid。

3. 实现三栏布局
   - 考点：圣杯布局、双飞翼布局、flex、grid。

4. 实现等高布局
   - 考点：flex stretch、grid、padding margin 技巧。

5. 实现粘性页脚
   - 考点：视口高度、flex column、内容不足时底部固定。

6. 实现 1px 边框
   - 考点：设备像素比、transform scale、伪元素。

7. 实现三角形
   - 考点：border 绘制、透明边框。

8. 实现多行文本省略
   - 考点：`line-clamp`、兼容性、纯 CSS 降级。

9. 实现响应式栅格
   - 考点：media query、CSS grid、minmax、auto-fit。

10. 实现瀑布流布局
    - 考点：CSS columns、grid masonry、JS 计算列高。

11. 实现移动端适配方案
    - 考点：rem、vw、viewport、设计稿换算。

12. 实现暗黑模式
    - 考点：CSS 变量、`prefers-color-scheme`、主题切换持久化。

13. 实现骨架屏
    - 考点：渐变动画、占位结构、加载状态。

14. 实现 loading 动画
    - 考点：keyframes、transform、动画性能。

15. 实现无障碍 Modal
    - 考点：焦点管理、ESC 关闭、遮罩点击、滚动锁定、ARIA。

## React 手撕

1. 手写 `useState`
   - 考点：hook 状态存储、更新触发渲染、调用顺序。

2. 手写 `useEffect`
   - 考点：依赖比较、副作用执行、清理函数。

3. 手写 `useMemo`
   - 考点：依赖缓存、引用稳定。

4. 手写 `useCallback`
   - 考点：函数缓存、依赖数组。

5. 手写 `useRef`
   - 考点：跨渲染保持引用、不触发渲染。

6. 手写 `usePrevious`
   - 考点：保存上一次值、effect 执行时机。

7. 手写 `useDebounce`
   - 考点：定时器、副作用清理、输入防抖。

8. 手写 `useThrottle`
   - 考点：节流状态、定时器、引用保存。

9. 手写 `useRequest`
   - 考点：loading、error、data、竞态取消、重试。

10. 手写 `useVirtualList`
    - 考点：滚动容器、可视范围、偏移量。

11. 手写简版 Redux
    - 考点：store、dispatch、subscribe、reducer。

12. 手写 `connect`
    - 考点：订阅 store、映射 state、触发更新。

13. 手写中间件机制
    - 考点：洋葱模型、函数组合、dispatch 增强。

14. 手写简版 React Router
    - 考点：路由匹配、History API、Context。

15. 手写 Error Boundary 示例
    - 考点：错误捕获边界、降级 UI、生命周期。

16. 手写受控表单组件
    - 考点：状态绑定、校验、提交、重置。

17. 手写 React Modal
    - 考点：Portal、滚动锁定、焦点管理。

18. 手写 React 无限滚动列表
    - 考点：IntersectionObserver、分页加载、重复请求控制。

## Vue 手撕

1. 手写 Vue2 响应式
   - 考点：`Object.defineProperty`、依赖收集、派发更新。

2. 手写 Vue3 响应式
   - 考点：Proxy、Reflect、依赖收集、嵌套对象。

3. 手写 `ref`
   - 考点：`.value`、依赖触发、基本类型响应式。

4. 手写 `computed`
   - 考点：懒执行、缓存、依赖失效。

5. 手写 `watch`
   - 考点：新旧值、立即执行、深度监听、清理副作用。

6. 手写 `nextTick`
   - 考点：微任务、异步更新队列、回调收集。

7. 手写事件总线
   - 考点：发布订阅、组件通信、销毁解绑。

8. 手写 Vue Router 简版
   - 考点：hash/history、路由表、组件渲染。

9. 手写 Pinia/Vuex 简版
   - 考点：集中状态、订阅更新、action、getter。

10. 手写 `v-model` 组件
    - 考点：props、emit、受控组件思想。

## 编译与工程化

1. 手写 Babel 插件
   - 考点：AST 解析、visitor、节点替换。

2. 手写 Webpack loader
   - 考点：源码转换、同步/异步 loader、source map。

3. 手写 Webpack plugin
   - 考点：compiler、compilation、hooks、资源处理。

4. 手写简版打包器
   - 考点：依赖分析、AST、模块图、运行时代码。

5. 手写 CommonJS 模块加载器
   - 考点：`require`、`module.exports`、模块缓存。

6. 手写 ESM 转 CommonJS
   - 考点：静态导入导出、AST 转换。

7. 手写热更新 HMR 简版
   - 考点：文件监听、WebSocket、模块替换。

8. 手写 Source Map 解析思路
   - 考点：映射关系、VLQ 编码、调试定位。

9. 手写 CLI 参数解析器
   - 考点：命令、选项、默认值、帮助信息。

10. 手写环境变量加载
    - 考点：`.env` 解析、优先级、模式区分。

## 网络与安全

1. 手写 JSONP
   - 考点：script 标签跨域、callback、清理节点。

2. 手写请求签名
   - 考点：参数排序、时间戳、nonce、防重放。

3. 手写前端鉴权流程
   - 考点：token 存储、刷新 token、路由守卫、并发刷新锁。

4. 手写刷新 token 队列
   - 考点：请求拦截、401 处理、队列重放、失败登出。

5. 手写 XSS 转义函数
   - 考点：HTML 实体编码、属性上下文、白名单。

6. 手写 CSRF 防护示例
   - 考点：SameSite、CSRF token、自定义 header。

7. 手写请求去重
   - 考点：请求 key、pending map、取消重复请求。

8. 手写接口缓存
   - 考点：缓存 key、过期策略、强制刷新。

9. 手写断网重连
   - 考点：online/offline 事件、重试队列、指数退避。

10. 手写 SSE 客户端封装
    - 考点：EventSource、重连、消息分发。

## 算法与数据结构

1. 两数之和
   - 考点：哈希表、一次遍历。

2. 三数之和
   - 考点：排序、双指针、去重。

3. 合并两个有序数组
   - 考点：双指针、原地合并。

4. 合并两个有序链表
   - 考点：链表指针、递归/迭代。

5. 反转链表
   - 考点：指针移动、递归。

6. 判断链表是否有环
   - 考点：快慢指针。

7. 找到环形链表入口
   - 考点：快慢指针相遇后重置。

8. 删除链表倒数第 N 个节点
   - 考点：双指针、虚拟头节点。

9. 有效括号
   - 考点：栈、括号映射。

10. 最小栈
    - 考点：辅助栈、常数时间取最小值。

11. 用两个栈实现队列
    - 考点：输入栈、输出栈、摊还复杂度。

12. 二叉树前序遍历
    - 考点：递归、栈迭代。

13. 二叉树中序遍历
    - 考点：递归、栈迭代。

14. 二叉树后序遍历
    - 考点：递归、双栈/反向前序。

15. 二叉树层序遍历
    - 考点：队列、按层收集。

16. 二叉树最大深度
    - 考点：DFS、BFS。

17. 判断对称二叉树
    - 考点：递归比较、队列比较。

18. 二叉树最近公共祖先
    - 考点：递归返回、搜索路径。

19. 岛屿数量
    - 考点：DFS、BFS、访问标记。

20. 最长不重复子串
    - 考点：滑动窗口、哈希表。

21. 最长回文子串
    - 考点：中心扩展、动态规划。

22. 最大子数组和
    - 考点：动态规划、贪心。

23. 买卖股票最佳时机
    - 考点：维护最低价格、最大利润。

24. 爬楼梯
    - 考点：动态规划、滚动变量。

25. 零钱兑换
    - 考点：完全背包、动态规划。

26. 最长递增子序列
    - 考点：动态规划、贪心加二分。

27. 快速排序
    - 考点：分区、递归、原地排序。

28. 归并排序
    - 考点：分治、合并有序数组。

29. 堆排序
    - 考点：建堆、下沉、原地排序。

30. 二分查找
    - 考点：边界条件、左右闭区间/左闭右开区间。

31. 搜索旋转排序数组
    - 考点：二分变形、有序半区判断。

32. Top K 高频元素
    - 考点：哈希统计、小顶堆、桶排序。

33. 字符串全排列
    - 考点：回溯、去重、路径选择。

34. 子集
    - 考点：回溯、选择与撤销。

35. 组合总和
    - 考点：回溯、剪枝、重复选择。

## 场景综合题

1. 实现一个支持搜索、分页、排序的表格组件
   - 考点：状态设计、受控参数、异步请求、空态与 loading。

2. 实现一个可拖拽排序列表
   - 考点：拖拽事件、占位、排序更新、移动端兼容。

3. 实现一个图片上传组件
   - 考点：格式校验、大小校验、预览、进度、失败重试。

4. 实现一个树形选择器
   - 考点：递归渲染、半选状态、父子联动、搜索过滤。

5. 实现一个表单校验器
   - 考点：规则配置、异步校验、错误收集、触发时机。

6. 实现一个前端权限系统
   - 考点：路由权限、按钮权限、菜单过滤、动态路由。

7. 实现一个低代码拖拽画布
   - 考点：组件协议、拖拽定位、选中态、属性面板、撤销重做。

8. 实现一个 Markdown 编辑器
   - 考点：输入同步、预览渲染、XSS 防护、快捷键。

9. 实现一个消息通知中心
   - 考点：队列、自动关闭、手动关闭、堆叠展示。

10. 实现一个多步骤向导
    - 考点：步骤状态、表单缓存、前后切换、提交汇总。

## 高频追问清单

1. 这个实现有哪些边界情况？
2. 时间复杂度和空间复杂度是多少？
3. 如果数据量扩大 100 倍，瓶颈在哪里？
4. 如何做单元测试？
5. 如何处理异常和降级？
6. 如何避免内存泄漏？
7. 浏览器兼容性如何？
8. 移动端有什么不同？
9. SSR 场景是否可用？
10. TypeScript 类型应该怎么设计？

## 练习建议

1. 第一轮：只写核心逻辑，保证功能正确。
2. 第二轮：补边界条件，例如空值、异常、重复输入、并发竞态。
3. 第三轮：补测试用例，用输入输出证明实现可靠。
4. 第四轮：练口述，把实现拆成“问题定义、核心思路、关键代码、复杂度、可扩展点”五段。

