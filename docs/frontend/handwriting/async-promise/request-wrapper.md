# 请求封装

1. 手写 ajax
   - 考点：XMLHttpRequest、readyState、状态码。
   - 边界：超时、abort、网络错误。

2. 手写 fetch 封装
   - 考点：请求拦截、响应拦截、统一错误。
   - 边界：非 2xx 状态、JSON 解析失败、取消请求。

3. 手写请求队列
   - 考点：排队、暂停、恢复、优先级。
   - 边界：队列销毁后不再执行。

4. 手写请求去重
   - 考点：请求 key、pending map、复用同一个 Promise。
   - 边界：参数序列化稳定性、请求结束清理。

