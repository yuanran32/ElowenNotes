# Vue 生态

1. 手写 Vue Router 简版
   - 考点：hash/history、路由表、组件渲染。
   - 边界：路由守卫、动态路由。

2. 手写 Pinia/Vuex 简版
   - 考点：集中状态、订阅更新、action、getter。
   - 边界：模块拆分、插件机制。

3. 手写 `v-model` 组件
   - 考点：props、emit、受控组件思想。
   - 边界：多个 v-model。

4. 手写 Vue 插件
   - 考点：install、全局属性、全局组件。
   - 边界：重复安装。

